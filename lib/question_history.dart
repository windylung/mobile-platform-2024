import 'package:flutter/cupertino.dart';

class QuestionHistory extends StatelessWidget {
  const QuestionHistory({super.key});

  @override
  Widget build(BuildContext context){
    return Container(
      child: Center(
        child: Text("문답 기록 페이지입니다"),
      ),
    );
  }
}